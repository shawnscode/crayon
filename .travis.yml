language: rust
rust:
  - nightly
  - stable
matrix:
  allow_failures:
    - rust: nightly

script:
    - cargo build --all --verbose
    - cargo test --all --verbose -- --ignored
    - cargo build --all --verbose --no-default-features
    - cargo test --all --verbose --no-default-features -- --ignored
